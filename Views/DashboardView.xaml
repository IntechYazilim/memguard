<UserControl x:Class="MemGuard.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MemGuard.Controls"
             xmlns:loc="clr-namespace:MemGuard.Localization"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource CardBgBrush}"
                    BorderBrush="{StaticResource BorderDarkBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="24,20"
                    Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Loc Key=Dashboard.Kicker}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="Bold" FontFamily="{StaticResource MainFont}"/>
                        <TextBlock Text="{Binding SystemStatus}" Foreground="{StaticResource TextWhiteBrush}" FontSize="28" FontWeight="Bold" Margin="0,4,0,0" FontFamily="{StaticResource MainFont}"/>
                    </StackPanel>

                    <controls:CircularHealthGauge Grid.Column="1"
                                                  Value="{Binding SystemHealth}"
                                                  Width="140"
                                                  Height="140"
                                                  HorizontalAlignment="Right" />
                </Grid>
            </Border>

            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,24">
                <Border Background="{StaticResource CardAltBgBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" CornerRadius="8" Padding="16,14" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Key=Dashboard.CpuUsage}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10.5" FontWeight="Bold" FontFamily="{StaticResource MainFont}" />
                        <TextBlock Text="{Binding CpuUsage, StringFormat={}{0:F1}%}" Foreground="{StaticResource TextWhiteBrush}" FontSize="22" FontWeight="Bold" Margin="0,4,0,0" FontFamily="{StaticResource MainFont}" />
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource CardAltBgBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" CornerRadius="8" Padding="16,14" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Key=Dashboard.RamUsage}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10.5" FontWeight="Bold" FontFamily="{StaticResource MainFont}" />
                        <TextBlock Text="{Binding RamUsage, StringFormat={}{0:F1}%}" Foreground="{StaticResource TextWhiteBrush}" FontSize="22" FontWeight="Bold" Margin="0,4,0,0" FontFamily="{StaticResource MainFont}" />
                        <TextBlock Text="{Binding RamText}" Foreground="{StaticResource TextMutedBrush}" FontSize="10" Margin="0,2,0,0" FontFamily="{StaticResource MainFont}" />
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource CardAltBgBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" CornerRadius="8" Padding="16,14" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Key=Dashboard.DiskUsage}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10.5" FontWeight="Bold" FontFamily="{StaticResource MainFont}" />
                        <TextBlock Text="{Binding DiskUsage, StringFormat={}{0:F1}%}" Foreground="{StaticResource TextWhiteBrush}" FontSize="22" FontWeight="Bold" Margin="0,4,0,0" FontFamily="{StaticResource MainFont}" />
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource CardAltBgBrush}" BorderBrush="{StaticResource BorderDarkBrush}" BorderThickness="1" CornerRadius="8" Padding="16,14" Margin="0">
                    <StackPanel>
                        <TextBlock Text="{loc:Loc Key=Dashboard.SystemUptime}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10.5" FontWeight="Bold" FontFamily="{StaticResource MainFont}" />
                        <TextBlock Text="{Binding Uptime}" Foreground="{StaticResource TextWhiteBrush}" FontSize="22" FontWeight="Bold" Margin="0,4,0,0" FontFamily="{StaticResource MainFont}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{StaticResource CardBgBrush}"
                        BorderBrush="{StaticResource BorderDarkBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="16,14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="{loc:Loc Key=Dashboard.RamPerformance}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,12" FontFamily="{StaticResource MainFont}" />
                        <controls:RealTimeLineChart Grid.Row="1"
                                                    Value="{Binding RamUsage}"
                                                    LineBrush="{StaticResource ElectricBlueBrush}"
                                                    Height="160" />
                    </Grid>
                </Border>

                <Border Grid.Column="2"
                        Background="{StaticResource CardBgBrush}"
                        BorderBrush="{StaticResource BorderDarkBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="16,14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="{loc:Loc Key=Dashboard.CpuPerformance}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,12" FontFamily="{StaticResource MainFont}" />
                        <controls:RealTimeLineChart Grid.Row="1"
                                                    Value="{Binding CpuUsage}"
                                                    LineBrush="{StaticResource NeonPurpleBrush}"
                                                    Height="160" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
