<UserControl x:Class="MemGuard.Controls.RealTimeLineChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400">
    <Grid Background="Transparent">
        <!-- Background Grid Lines -->
        <Canvas x:Name="GridLinesCanvas" IsHitTestVisible="False">
            <Line x:Name="GridLine75" Stroke="{DynamicResource BorderDarkBrush}" StrokeThickness="1" StrokeDashArray="4 4" />
            <Line x:Name="GridLine50" Stroke="{DynamicResource BorderDarkBrush}" StrokeThickness="1" StrokeDashArray="4 4" />
            <Line x:Name="GridLine25" Stroke="{DynamicResource BorderDarkBrush}" StrokeThickness="1" StrokeDashArray="4 4" />
        </Canvas>
        
        <Canvas x:Name="ChartCanvas" ClipToBounds="True">
            <!-- Glow background fill path -->
            <Path x:Name="AreaPath" Opacity="0.15">
                <Path.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop x:Name="AreaGradientStart" Color="{DynamicResource ElectricBlueColor}" Offset="0" />
                        <GradientStop x:Name="AreaGradientEnd" Color="Transparent" Offset="1" />
                    </LinearGradientBrush>
                </Path.Fill>
            </Path>

            <!-- Glow line path (blurred background layer) -->
            <Path x:Name="GlowLinePath" StrokeThickness="4" StrokeEndLineCap="Round" StrokeStartLineCap="Round" Opacity="0.4">
                <Path.Effect>
                    <BlurEffect Radius="6" />
                </Path.Effect>
            </Path>

            <!-- Active sharp neon line path (foreground layer) -->
            <Path x:Name="LinePath" StrokeThickness="2" StrokeEndLineCap="Round" StrokeStartLineCap="Round" />
        </Canvas>

        <!-- Current Value Overlay Badge (Top Right) -->
        <Border Background="{DynamicResource CardAltBgBrush}"
                BorderBrush="{DynamicResource BorderDarkBrush}"
                BorderThickness="1"
                CornerRadius="4"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="8"
                Padding="6,3"
                IsHitTestVisible="False">
            <TextBlock x:Name="CurrentValueText"
                       Text="0%"
                       Foreground="{DynamicResource TextWhiteBrush}"
                       FontSize="11"
                       FontWeight="Bold"
                       FontFamily="{StaticResource MainFont}"/>
        </Border>
    </Grid>
</UserControl>
